The following repository is a program called ActsType, a Bible verse memorization app. Your job is to refactor the codebase and to make it ready to be used as a website that people can access.

# Task Overview

1) Familiarize yourself with the codebase 
2) Refactor the ENTIRE codebase to use typescript, then TEST the werbsite using existing tests (rewrite if needed) to ensure functionality.
3) Set up redux for state management 
4) Set up auth with login and sign up page, set up auth slice for global state management, using supabase authentication. (Use google auth--I only want users with a certain @ domain to be able to access the website)

Don't work on all this yet. This is for context. Lets start the task after you are familiarized.

# RULES
- DO NOT read ALL of esv.json. It's a really large file that includes all of the bible. Read portions of it to understand the json format if needed, but not the whole thing.

- Make sure NONE of the features cahnge when porting over to typescript. ALL functionality, including UI design must remain the same.

- Upon every prompt, before you start making changes, READ ALL the files in the directory, as I may have made changes/added new files since the last prompt

- Ask questions if anything is not clear.


Thank you for working on this!



